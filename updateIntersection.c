#include "updateIntersection.h"

void updateIntersection(car** linkedListIntersection, int*** intersection) {
    // convert linked list to 3D array for the AI
    // loop through the intersection and set everything to 0

    // loop through the linked list and update each lane where there is a car
        // make sure to take into account the length of the car

    // make sure not to free the linked list, or create a new one

}